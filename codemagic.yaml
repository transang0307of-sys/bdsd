workflows:
  android-workflow:
    name: FAKE BDSD MBBANK 
    max_build_duration: 30
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android Debug APK
        script: |
          # Cài đặt gradle nếu thiếu file thực thi
          gradle assembleDebug
    artifacts:
      - app/build/outputs/apk/**/*.apk
    publishing:
      email:
        recipients:
          - kaizcontact23@gmail.com
          
